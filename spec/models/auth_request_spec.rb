require 'rails_helper'

RSpec.describe AuthRequest, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:auth_request)
  #   expect(record).to be_persisted
  # end
end
